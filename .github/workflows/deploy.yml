name: DevZip CI/CD Pipeline (Optimized)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  JAVA_VERSION: '17'
  NODE_VERSION: '18'  # package.json과 일치

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'temurin'

    - name: Set up Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: src/main/frontend/package-lock.json

    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Build Application
      env:
        CI: false
        GENERATE_SOURCEMAP: false
      run: |
        chmod +x gradlew
        ./gradlew clean build -x test

        # JAR 파일 확인
        ls -lh build/libs/
        echo "✅ Build completed"

    - name: Deploy JAR to Production Server
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        port: ${{ secrets.SERVER_SSH_PORT }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        source: "build/libs/*.jar"
        target: "/home/ubuntu/project/devzip/build/libs/"
        timeout: 5m

    - name: Restart Docker Services
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        port: ${{ secrets.SERVER_SSH_PORT }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        timeout: 60s
        command_timeout: 10m
        script: |
          cd /home/ubuntu/project/devzip
          echo "🚀 Starting deployment..."

          # Git 저장소 최신화
          echo "📥 Updating git repository..."
          git config pull.rebase false
          git pull origin master
          echo "✅ Git repository updated"

          # JAR 파일 확인 (개선된 로직)
          JAR_COUNT=$(ls -1 build/libs/*.jar 2>/dev/null | wc -l)
          if [ "$JAR_COUNT" -eq 0 ]; then
            echo "❌ JAR file not found!"
            exit 1
          fi

          echo "✅ JAR file verified ($JAR_COUNT files found)"
          ls -lh build/libs/

          # Docker 재시작 및 캐시 무효화 (sudo 권한으로 실행)
          echo "🐳 Stopping Docker containers..."
          sudo docker compose down

          echo "🗑️ Removing old images to ensure fresh build..."
          sudo docker compose rm -f
          sudo docker images | grep devzip | awk '{print $3}' | xargs -r sudo docker rmi -f || true

          echo "🔨 Building Docker images without cache..."
          sudo docker compose build --no-cache

          echo "🚀 Starting Docker containers with force recreate..."
          sudo docker compose up -d --force-recreate

          echo "⏳ Waiting for application to start (increased wait time due to fresh build)..."
          sleep 30

          # 헬스체크
          for i in {1..12}; do
            if curl -f http://localhost:8080/actuator/health > /dev/null 2>&1; then
              echo "✅ Application started successfully!"
              exit 0
            fi
            echo "⏳ Waiting... ($i/12)"
            sleep 3
          done

          echo "⚠️ Application might be starting slower than expected"
          echo "🎉 Deployment completed!"

  health-check:
    name: Health Check & Verification
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: github.ref == 'refs/heads/master' && github.event_name == 'push' && !contains(github.event.head_commit.message, '[skip-health-check]')

    steps:
    - name: Wait for Application Startup
      run: |
        echo "⏳ Waiting for application to fully start..."
        echo "📊 Docker start-period is 180s, waiting sufficiently..."
        sleep 90

    - name: Health Check - Basic Connectivity
      run: |
        echo "🏥 Running health checks with retry logic..."

        max_retries=10
        retry_count=0
        wait_time=10

        while [ $retry_count -lt $max_retries ]; do
          response=$(curl -s -o /dev/null -w "%{http_code}" https://devzip.cloud/actuator/health || echo "000")

          if [ "$response" = "200" ]; then
            echo "✅ Health check endpoint: OK (attempt $((retry_count + 1))/$max_retries)"
            break
          else
            retry_count=$((retry_count + 1))
            if [ $retry_count -lt $max_retries ]; then
              echo "⏳ Health check failed (HTTP $response), retrying in ${wait_time}s... (attempt $retry_count/$max_retries)"
              sleep $wait_time
            else
              echo "❌ Health check endpoint failed after $max_retries attempts (HTTP $response)"
              exit 1
            fi
          fi
        done

    - name: Authentication System Check
      run: |
        echo "🔐 Testing JWT authentication system..."

        login_response=$(curl -s -o /dev/null -w "%{http_code}" -X POST https://devzip.cloud/api/auth/signin \
          -H "Content-Type: application/json" \
          -d '{"username":"test","password":"test"}' || echo "000")

        if [ "$login_response" = "403" ] || [ "$login_response" = "401" ] || [ "$login_response" = "400" ]; then
          echo "✅ Authentication endpoint: Responding properly"
        elif [ "$login_response" = "404" ]; then
          echo "❌ Authentication endpoint: Not found"
        else
          echo "⚠️ Authentication endpoint: Unexpected response ($login_response)"
        fi

    - name: Frontend Loading Check
      run: |
        echo "🌐 Testing frontend loading..."

        main_response=$(curl -s -o /dev/null -w "%{http_code}" https://devzip.cloud/ || echo "000")

        if [ "$main_response" = "200" ]; then
          echo "✅ Main page: Loading successfully"
        else
          echo "❌ Main page: Failed to load (HTTP $main_response)"
        fi

    - name: Protected Routes Check
      run: |
        echo "🛡️ Testing protected routes..."

        dashboard_response=$(curl -s -o /dev/null -w "%{http_code}" https://devzip.cloud/dashboard || echo "000")
        traceboard_response=$(curl -s -o /dev/null -w "%{http_code}" https://devzip.cloud/traceboard || echo "000")

        if [ "$dashboard_response" = "200" ] || [ "$dashboard_response" = "302" ] || [ "$dashboard_response" = "401" ]; then
          echo "✅ Dashboard protection: Working"
        else
          echo "⚠️ Dashboard protection: Unexpected response ($dashboard_response)"
        fi

        if [ "$traceboard_response" = "200" ] || [ "$traceboard_response" = "302" ] || [ "$traceboard_response" = "401" ]; then
          echo "✅ Traceboard protection: Working"
        else
          echo "⚠️ Traceboard protection: Unexpected response ($traceboard_response)"
        fi

    - name: Final Status Report
      run: |
        echo ""
        echo "📊 Deployment Status Summary:"
        echo "=========================================="
        echo "🎯 Application: DevZip"
        echo "🌐 URL: https://devzip.cloud"
        echo "🔐 Authentication: JWT-based admin system"
        echo "🛡️ Protected Routes: Dashboard, Traceboard"
        echo "⚡ Features: Real-time analytics, User tracking"
        echo ""
        echo "✅ Deployment completed successfully!"
        echo "🚀 All systems operational and ready for use!"
